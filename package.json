{
  "name": "smart-default-bookmark-folder",
  "version": "0.0.1",
  "description": "Guesses the best folder for new bookmarks.",
  "scripts": {
    "build": "webpack && npx web-ext build --source-dir=build --artifacts-dir=dist --overwrite-dest",
    "dev": "webpack --watch"
  },
  "type": "module",
  "author": "Camille Van Hoffelen",
  "license": "MIT",
  "dependencies": {
    "@huggingface/transformers": "^3.7.2",
    "@mozilla/readability": "^0.6.0"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^13.0.1",
    "ignore-loader": "^0.1.2",
    "web-ext-plugin": "^2.11.0",
    "webpack": "^5.101.3",
    "webpack-cli": "^6.0.1"
  }
}
